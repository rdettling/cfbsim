{
    "name": "cfbsim",
    "engines": {
        "node": "20.x",
        "npm": "10.x"
    },
    "scripts": {
        "postinstall": "cd frontend && npm install && npm run build && mkdir -p ../backend/templates && cp dist/index.html ../backend/templates/ && cd ../backend && python manage.py collectstatic --noinput",
        "start": "cd backend && python manage.py runserver",
        "heroku-postbuild": "cd frontend && npm install && npm run build"
    }
}